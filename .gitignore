# --- Build artifacts ---
*.exe
*.exe~
ai-gateway
ai-gateway.exe
ai-gateway-linux-amd64

# --- Project-internal agent memory / tooling (not for public repo) ---
.workbuddy/

# --- Runtime data (SQLite WAL + shm + backups) ---
# /data/          : 容器内路径，若改用 bind mount 备查
# /gateway-data/  : docker-compose 注释里给出的 bind mount 替代目录
/data/
/gateway-data/

# --- Logs ---
*.log

# --- 生成的文档 / 排查记录（不入库）---
*.docx
*.pdf

# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.swp
